import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App /> {/*app é o component principal da minha aplicação*/}
  </React.StrictMode>,
  document.getElementById('root')
);

